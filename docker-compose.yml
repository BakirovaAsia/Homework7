version: "3"
services:
  pack:
    build: ./pack
    volumes:
      - web-dir:/usr/local/webapps
  webapp:
    build: ./prod
    depends_on:
      - pack
    ports:
      - 8000:8000
    volumes:
      - web-dir:/tmp
    

volumes:
  web-dir: